<div class='container-fluid'>
  {{main-nav}}
  {{#each flashMessages.queue as |flash|}}
    {{flash-message flash=flash}}
  {{/each}}
  {{outlet}}
  {{#if session.isAuthenticated}}
    <div class='col-xs'>
      logged in as {{session.currentUser.username}}
    </div>
  {{/if}}
</div>
